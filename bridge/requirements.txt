# Core (always required)
aiohttp>=3.9,<4
smbus2>=0.4.1   # LCD1602 I2C display
smbus>=1.1.post2  # required by pidog/rgb_strip on Python 3.13
gpiozero>=2.0     # required by robot_hat / pidog on Python 3.13

# Local voice (Vosk STT + Piper TTS) — install for PIDOG_VOICE_PROVIDER=local
vosk>=0.3.45
sounddevice>=0.4
numpy>=1.24

# TOML config support for Python <3.11
tomli>=2.0; python_version < "3.11"

# --- Cloud voice (PIDOG_VOICE_PROVIDER=pipecat) ---
# No extra packages needed for deepgram/azure/google/groq — direct REST via aiohttp.
# Sarvam AI STT requires the SDK:
sarvamai>=0.1.0   # pip install sarvamai  (for stt_provider = "sarvam")
# Set PIPECAT_STT_API_KEY and PIPECAT_TTS_API_KEY before running.
# Supported STT: deepgram, sarvam, groq, azure, google
# Supported TTS: cartesia, elevenlabs, azure, google
